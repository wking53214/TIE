# Typed Handoff

`TypedHandoff` carries objective, source identity, evidence IDs, artifact IDs, known uncertainty, routing signal, and provenance.

Routing is represented as data and cannot trigger downstream execution.
