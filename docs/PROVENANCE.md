# Provenance

TIE preserves source identity, source location, conversation/message/segment references, extraction origin, transformation origin, origin kind, and optional version/lineage identifiers.

The repository does not claim cryptographic immutability, hashing, append-only persistence, or database-backed provenance because those mechanisms were not recovered from the supplied summaries.
