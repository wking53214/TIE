# Schemas

The exact historical TIE_PACKAGE and EvidenceRecord schemas were not recoverable from the five summaries.

Runtime dataclasses in `src/tie/models/core.py` constitute the reconstructed implementation baseline. They are not represented as exact historical schema recovery.
